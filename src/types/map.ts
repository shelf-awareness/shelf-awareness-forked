export interface LocationMarker {
  id: number;
  name: string;
  address: string;
  latitude: number;
  longitude: number;
}

export interface GroceryStore {
  id: number;
  name: string;
  lat: number;
  lng: number;
}